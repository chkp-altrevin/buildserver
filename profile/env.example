# File Guidelines
# Assumes this deployment will be used as a workshop or lab local setup.
# If you plan to use in production or make public, use a vault, password manager etc.
# Use "renv" to refresh this file. Use "denv" to delete all entries. Use renv to bring them back
# Teller and Pass are included for an easy transition or bring your own
#
# ==============================================================
#
# everything else below is optional or related to the demos with comments to help guide you
#
# optional additional git and image repos. as of now these are not referenced in any of the demo scripts
# only here for convenience while using the scripts/git_auth_setup.sh and this file

# GitHub Credentials
export GITHUB_USER=
export GITHUB_TOKEN=

# GitLab Credentials
export GITLAB_USER=
export GITLAB_TOKEN=

# GitLab Private Credentials
export GITLAB_PRIVATE_USER=
export GITLAB_PRIVATE_TOKEN=
export GITLAB_PRIVATE_DOMAIN=

# Bitbucket Credentials
BITBUCKET_USER=
BITBUCKET_TOKEN=

# Private Registry (Example)
PRIVATE_USER=
PRIVATE_TOKEN=

# Other Git not defined planning for future use
export HARBOR_TOKEN=
export AWS_ECR_TOKEN=
export AZURECR_TOKEN=
#
# optional additional provider access
# required if using the demo deployment scripts
# aws env used for cft and tf deployments
export AWS_ACCESS_KEY_ID="ASXXXXXXXXXXXX"
export AWS_SECRET_ACCESS_KEY="xxxxxxxxxxxxxxxxxxxx"
export AWS_SESSION_TOKEN="xxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#
# optional linode
export LINODE_TOKEN=
export LINODE_STACKSET=
export LINODE_REGION=
#
# optional letsencrypt - if you plan to deploy the lab dokploy or nging proxy manager
export LETSENCRYPT_EMAIL=
export LETSENCRYPT_DOMAIN=
#
# optional openai - one of the repos downloaded makes use of this
export OPENAI_TOKEN=
#
# optional package mgmnt - because i dev on side
export NPM_TOKEN=
export NVM_DEFAULT_VERSION=
#
# password mgmnt - setting up pass
export PASS_TOKEN=
#
# vercel edge deployments - give it a try
export VERCEL_TOKEN=
#
# optional docker env vars
# required for image scan and use case, yes you can modify
export PUID=1000
export PGID=1000
export TZ="America/Chicago"
#
# optional Traefik and LE related deployments in resources folder
# export DOMAINNAME_CLOUD_SERVER=local.yourdomain.org
# export DOMAINNAME2=null.yourdomain.org
#
# optional Cloudflare related for warp or tunneling domains
export CLOUDFLARE_EMAIL=youremail@youremail.com
export CLOUDFLARE_API_KEY=XXXXXXXXXXX
export LOCAL_IPS=127.0.0.1/32,10.0.0.0/8,192.168.0.0/16,172.16.0.0/12
export CLOUDFLARE_IPS=173.245.48.0/20,103.21.244.0/22,103.22.200.0/22,103.31.4.0/22,141.101.64.0/18,108.162.192.0/18,190.93.240.0/20,188.114.96.0/20,197.234.240.0/22,198.41.128.0/17,162.158.0.0/15,104.16.0.0/13,104.24.0.0/14,172.64.0.0/13,131.0.72.0/22
#
